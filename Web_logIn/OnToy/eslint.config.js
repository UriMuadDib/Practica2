/* export default {
    rules: {
        'no-unused-vars': ['error', { vars: 'all', args: 'none', ignoreRestSiblings: false }],
        'camelcase': 'error', // Requiere que las variables sean en camelCase
        'func-style': ['error', 'declaration', { allowArrowFunctions: true }], // Permitir funciones en forma de expresión
        'quotes': ['error', 'single'], // Usar comillas simples
        'semi': ['error', 'always'], // Requerir punto y coma
        'no-console': 'warn', // Advertir sobre el uso de console.log
        'react/prop-types': 'off', // Desactivar la regla de PropTypes en React
    },
  };
   */